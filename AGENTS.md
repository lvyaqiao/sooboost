# AGENTS.md — Sooboost 开发规约

> 仓库结构：**多分支子目录制**（对标 WhisperGrass）——每个分支 = 一个子目录内的独立 clone，不用 git worktree。现行仅 `master/` 一个分支目录；未来分支协作时，基于同一 remote（https://github.com/lvyaqiao/sooboost.git）新建分支（分支前缀 `<域>-<任务>/`），克隆为新的子目录即可。
> 文档入口：`doc/README.md`（目录导航/类型定义/写入路由表/域路由表/治理规则——开工前必读）。

## 文档体系

- doc/baseline/：契约层（product 需求 / tech 技术选型 / architecture 架构 ADR 决策表 D1-D10 + 设计红线 / contracts 可测试契约）——基线改版走 ADR 式
- doc/ledgers/：台账层（roadmap 里程碑+状态**权威单点** / backlog 待裁决需求池 / bugs BUG 分族七家族谱系 / risks 风险台账）
- doc/records/：流水层（shared 共享面登记+手动验收总表 / master 域交付与验收——交付登记处）
- doc/plans/：模块方案（实施完归档 doc/archive/）
- doc/research/：参考调研（GBDT 三巨头功能/工程/架构对错评价 + bug 分族与代码质量）
- doc/roles/：人员工作区（现行单域 master；mailbox 跨域声明 + taskbox 任务分派为未来分支协作留位）
- doc/archive/：冻结区（编号制历史文档 + 06 架构重构依据——只读，引用用摘要+指针）

## 常用命令

（代码初始化后填写：cargo test / cargo clippy / 基准门禁等，测试纪律见下）

## 测试纪律（契约与测试纪律 §二 迁移）

### 五类测试入 CI（红线 6 的操作化）

| 类别 | 工具 | 守护什么 | 何时必须新增 |
| --- | --- | --- | --- |
| 确定性测试 | 逐位比对 | 红线 3 可复现性 | 任何精度/并行改动 |
| 属性测试 | proptest | 不变量（如排序稳定性、bin 单调性、sketch 有界） | 新算法、新数据结构 |
| 快照回归 | canondata 思路 | 模型序列化/解析兼容 | 格式变更、序列化改动 |
| fuzz | cargo-fuzz | 解析/反序列化/边界输入不崩溃 | 任何读入外部字节的路径 |
| 基准门禁 | bench CI | 训练/推理性能不回归 | 优化与正确性耦合的改动 |

### 测试层级约定

- 单测：模块级单元行为（ut 目录镜像 src）。
- 属性测试：不可手写穷举的通用不变量。
- fuzz：输入是外部字节流的地方（模型加载、arrow 读取、bin 表解析）。
- 集成：训练→预测→序列化→再预测的端到端回路。
- 基准：训练时间、推理吞吐，纳入 CI 门禁而非本地自测。

### 变更 checklist（每次改动对照）

- [ ] 新增功能是否同步了测试面（前瞻式，不是 bug 后补）？
- [ ] 是否涉及精度/并行/顺序 → 确定性测试跑了吗？
- [ ] 是否涉及读入外部字节 → fuzz target 补了吗？
- [ ] 是否涉及模型格式 → 版本头升了吗？快照回归过了吗？
- [ ] 是否涉及性能敏感路径 → 基准门禁过了吗？
- [ ] 是否引入了新的 unsafe → 红线 7 审查注释写了吗？
- [ ] 是否动了全局状态 → 红线 4 违例检查？

## 易踩坑（原 09-工程注意事项迁移，条目编号稳定，删除需注明原因）

1. **并发纪律**：以 rayon 数据并行为主，禁止手工 `std::thread` 管理共享可变状态。杜绝全局可变状态（static mut / 全局配置）；一切运行时状态经 `TrainingContext` 显式传递（红线 4）。教训来源：LightGBM OpenMP 全局线程数副作用（#4705/#5102 双 issue）、XGBoost 手写并行 data race 慢性病。
2. **性能回归需要门禁，否则不可见**：优化与正确性耦合的改动（如 SIMD、缓存分块）必须过基准门禁。教训来源：三家性能回归族都缺门禁；LightGBM 提交量中几乎无性能回归修复——不代表没有，只是看不见。
3. **sketch / 分箱参数固化，不反复调**：quantile sketch 的内存估计、safety factor 等参数一旦验证确定就固化，禁止跟随 issue 反复微调。教训来源：XGBoost 分布式 sketch 多次调整 safety factor，说明参数未沉淀为契约。
4. **类别特征是新兴 bug 温床**：OOV（训练未见类别）、UTF-8、nullable 类别、高基数四类边界必须显式测试。教训来源：XGBoost 2025 默认启用类别特征后 OOV/UTF-8/nullable 密集暴露；三家类别特征族持续修复。
5. **缺失值/NaN 边界多为静默错误**：缺失值语义错误往往不崩溃、不报错，只产生错误结果——测试必须显式断言结果而非只看"没崩"。教训来源：LightGBM NaN 输入导致 UB、NaN bin 最高频时出错。
6. **加权/量化路径中间量用双精度**：加权分位数、回归目标统计量等聚合路径，中间量保持 f64，仅在最终输出降精度。教训来源：LightGBM 加权分位数两年内错两次（#5848/#7224），属静默精度错误。
7. **绑定层不反哺架构**：核心库纯 Rust + 可选薄 C ABI；任何语言绑定（Python 等）都是附属品，禁止为其改动核心 API 形状。教训来源：三巨头绑定层修复量 ≈ 核心 C++ 修复量；LightGBM API 兼容修复占比 31%。
8. **依赖克制**：不进跟随上游发版节奏的依赖（GPU 计算栈、底层容器库）；选依赖时问"它是否按月强迫我升级"。教训来源：XGBoost 被 CCCL/cuDF 月更拖累，持续"Fix compilation with latest XXX"。
9. **平台矩阵是负担放大器**：MVP 锁 x86_64 Linux/Windows；不预埋平台适配层。需要时再加，且新增平台必须同步 CI 矩阵。教训来源：CatBoost 非主流平台测试事后补齐、问题在用户侧爆发。
10. **错误处理显式化**：解析/反序列化/IO 错误必须显式传播；禁止 `unwrap` 于库代码（CLI 除外）、禁止忽略错误码。教训来源：CatBoost 零拷贝加载未检查 Skip 导致静默损坏。
11. **提交与文档纪律**："修复"提交与 typo/风格提交分开（三巨头中 catboost 约 1/4 修复是 typo 噪音，严重污染统计与 blame）。本项目文档按 doc/README.md 目录化体系维护；新经验先入本文件（易踩坑），再视情况升级为红线或契约（升级时注明动机）。

## 协作机制（多分支留缝）

- **现行单域**：master/ 内直接开发；里程碑状态权威见 doc/ledgers/roadmap.md，交付登记见 doc/records/master.md
- **分支纪律（未来多域时启用）**：工作分支一律 `<域>-<任务>/` 前缀，本地 merge 合入 master（收口人 = 维护者）：域 owner 推分支 + mailbox 投「可合入」→ 维护者合入（merge --no-ff + 验证门禁 + 推送 + 删分支）
- **跨域声明**：动他人域文件开工前投 doc/roles/mailbox/ 对应收件箱（投递即完成声明，不等批复）；任务分派走 doc/roles/taskbox/
- **阻塞 → debug 模式**：本域待办全阻塞时自动进入，只找不修，候选登记 doc/ledgers/bugs.md（登记前先查家族雷达防重复，根因分类必填：考虑不周 vs 架构缺陷）

## 参考

- **文档索引与路由**：`doc/README.md`（唯一入口）——引用格式 `doc/<目录>/<文件>.md §章节`
- 参考代码库：`reference/`（gitignore 排除，只读，许可证见各仓库；调研结论见 doc/research/）
